<h1>Log in</h1>

<%= form_for :session, url: login_path, method: :post do |f| %>
	<%= f.label :email %>
	<%= f.email_field :email %>
	<%= f.label :password %>
	<%= f.password_field :password %>
	<%= f.label :remember %>
	<%= f.check_box :remember %>
	<%= f.submit :submit %>

<% end %>